function Check() {
    var na = document.getElementById("name").value;
    if (na == "" || na == null) {
        alert("Enter name to download documents..");
    }
    else {
        alert("documents downloaded successfully....");
    }
}